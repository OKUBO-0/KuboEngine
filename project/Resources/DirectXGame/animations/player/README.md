# Player animation assets

| Local file | Source |
| --- | --- |
| `standing_idle.fbx` | Mixamo `Standing Idle`, downloaded 2026-08-04 |

This clip is kept as a retargeting candidate. The game currently uses the
embedded `idle` clip from `cube_world/cube_guy.glb` for the player idle pose.
`idle_hold` is kept as a fallback only because it narrows the stance too much
for the normal player idle.
